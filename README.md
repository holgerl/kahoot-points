# kahoot-points

Try it here: <https://holgerl.github.io/kahoot-points/>

## Build

`npm install`
`webpack`

Output in `/docs`

## Run dev-server

`npm install`
`npm start`

Listens on `localhost:8081`